# 🧠 АйТиПлюс — Образовательная платформа

Образовательная платформа для обучения программированию детей и подростков. Включает лендинг, личный кабинет студента и админ-панель для управления курсами, пользователями, заданиями и материалами.

## 🚀 Быстрый старт

### Требования

- PHP 8.2+
- Composer
- Node.js 18+ и npm
- MySQL 8.0+ (или SQLite для разработки)
- Laravel Herd (рекомендуется) или другой локальный сервер

### Установка

1. **Клонируйте репозиторий** (если еще не сделано):
```bash
git clone <repository-url>
cd code_school
```

2. **Установите зависимости PHP**:
```bash
composer install
```

3. **Установите зависимости Node.js**:
```bash
npm install
```

4. **Настройте окружение**:
```bash
cp .env.example .env
php artisan key:generate
```

5. **Настройте базу данных в `.env`**:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=code_school
DB_USERNAME=root
DB_PASSWORD=
```

6. **Создайте базу данных**:
   - Если используете Herd: база данных создается автоматически
   - Или создайте вручную через MySQL:
```sql
CREATE DATABASE code_school;
```

7. **Запустите миграции**:
```bash
php artisan migrate
```

8. **Заполните базу данных тестовыми данными**:
```bash
php artisan db:seed --class=StudentDataSeeder
```

   > **Важно:** Первый пользователь в базе автоматически получает права администратора.

9. **Соберите фронтенд**:
```bash
npm run build
```

   Или для разработки с hot-reload:
```bash
npm run dev
```

10. **Запустите приложение**:
   - Если используете Herd: приложение доступно по адресу `http://code_school.test`
   - Или запустите встроенный сервер:
```bash
php artisan serve
```

### Доступ к приложению

- **Главная страница**: `http://code_school.test/` или `http://localhost:8000/`
- **Вход**: `http://code_school.test/login`
- **Регистрация**: `http://code_school.test/register`
- **Личный кабинет студента**: `http://code_school.test/student/dashboard`
- **Админ-панель**: `http://code_school.test/admin/dashboard`

### Тестовые учетные данные

После выполнения сидера создается тестовый пользователь:
- **Email**: `student@example.com`
- **Пароль**: `password`
- **Роль**: Первый пользователь получает роль `admin`

## 📚 Структура проекта

```
code_school/
├── app/
│   ├── Http/
│   │   ├── Controllers/
│   │   │   ├── Admin/          # Контроллеры админ-панели
│   │   │   ├── Student/        # Контроллеры личного кабинета
│   │   │   └── LegalController.php
│   │   └── Middleware/
│   │       └── EnsureUserIsAdmin.php
│   └── Models/                 # Eloquent модели
├── database/
│   ├── migrations/             # Миграции базы данных
│   └── seeders/               # Сидеры для тестовых данных
├── resources/
│   ├── js/
│   │   ├── components/         # Vue компоненты
│   │   │   ├── landing/       # Компоненты лендинга
│   │   │   └── student/       # Компоненты личного кабинета
│   │   ├── layouts/
│   │   │   ├── admin/         # Layout админ-панели
│   │   │   ├── auth/          # Layout страниц авторизации
│   │   │   └── student/       # Layout личного кабинета
│   │   └── pages/
│   │       ├── admin/         # Страницы админ-панели
│   │       ├── auth/          # Страницы авторизации
│   │       ├── student/       # Страницы личного кабинета
│   │       └── Legal/         # Политика конфиденциальности и оферта
│   └── views/
│       └── certificates/      # Шаблоны сертификатов
├── routes/
│   └── web.php                # Веб-маршруты
└── public/                    # Публичные файлы
```

## 🛠️ Разработка

### Команды для разработки

**Сборка фронтенда:**
```bash
npm run build          # Production сборка
npm run dev           # Development с hot-reload
```

**Работа с базой данных:**
```bash
php artisan migrate              # Запуск миграций
php artisan migrate:fresh        # Сброс и запуск миграций
php artisan db:seed              # Заполнение тестовыми данными
php artisan migrate:fresh --seed # Сброс, миграции + сидер
```

**Очистка кэша:**
```bash
php artisan config:clear
php artisan cache:clear
php artisan view:clear
```

### Создание нового пользователя-администратора

Если нужно создать нового админа вручную:
```bash
php artisan tinker
```
```php
$user = \App\Models\User::create([
    'name' => 'Admin',
    'email' => 'admin@example.com',
    'password' => bcrypt('password'),
    'role' => 'admin',
]);
```

## 📦 Технологии

- **Backend**: Laravel 12
- **Frontend**: Vue.js 3 + Inertia.js
- **Стилизация**: Tailwind CSS
- **База данных**: MySQL
- **PDF генерация**: barryvdh/laravel-dompdf
- **Аутентификация**: Laravel Fortify

## 🔐 Роли пользователей

- **user** — Студент (доступ к личному кабинету)
- **admin** — Администратор (доступ к админ-панели)

## 📝 Основные функции

### Для студентов:
- Просмотр курсов и прогресса обучения
- Выполнение домашних заданий
- Чат с куратором
- Доступ к материалам курсов
- Получение и скачивание сертификатов

### Для администраторов:
- Управление курсами (создание, редактирование, удаление)
- Управление пользователями
- Управление заданиями
- Управление материалами
- Просмотр статистики

## 🐛 Решение проблем

### Ошибка подключения к базе данных

1. Проверьте настройки в `.env`
2. Убедитесь, что база данных создана
3. Проверьте права доступа MySQL пользователя
4. Очистите конфигурацию: `php artisan config:clear`

### Ошибки при сборке фронтенда

1. Удалите `node_modules` и `package-lock.json`
2. Выполните `npm install` заново
3. Проверьте версию Node.js: `node --version` (должна быть 18+)

### Страницы не загружаются (404)

1. Убедитесь, что выполнили `npm run build`
2. Проверьте, что сервер запущен
3. Очистите кэш: `php artisan config:clear && php artisan cache:clear`

## 📄 Лицензия

Проект разработан для образовательных целей.

---

# 🧠 АйТиПлюс — Лендинг

Современный лендинг для школы программирования для детей "АйТиПлюс" на стеке **Laravel + Vue.js + Tailwind CSS**.

## 🛠️ Технологический стек

- **Laravel** — PHP фреймворк
- **Vue.js 3** — JavaScript фреймворк
- **Tailwind CSS** — Utility-first CSS фреймворк
- **Inertia.js** — Мост между Laravel и Vue.js
- **Vite** — Сборщик фронтенда

## 📋 Структура проекта

```
code_school/
├── resources/
│   ├── js/
│   │   ├── pages/
│   │   │   └── Landing.vue          # Главная страница лендинга
│   │   └── components/
│   │       └── landing/             # Компоненты секций
│   │           ├── LandingHeader.vue
│   │           ├── HeroSection.vue
│   │           ├── WhyUsSection.vue
│   │           ├── HowItWorksSection.vue
│   │           ├── CoursesSection.vue
│   │           ├── TeachersSection.vue
│   │           ├── ReviewsSection.vue
│   │           ├── WhyProgrammingSection.vue
│   │           ├── ScheduleSection.vue
│   │           ├── FormSection.vue
│   │           ├── ContactsSection.vue
│   │           └── LandingFooter.vue
│   └── css/
│       └── app.css                  # Tailwind CSS стили
├── routes/
│   └── web.php                      # Роуты Laravel
├── tailwind.config.js               # Конфигурация Tailwind
└── vite.config.ts                   # Конфигурация Vite
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# PHP зависимости
composer install

# Node.js зависимости
npm install
```

### 2. Настройка окружения

Скопируйте `.env.example` в `.env` и настройте:

```bash
cp .env.example .env
php artisan key:generate
```

### 3. Запуск проекта

```bash
# В первом терминале - Laravel сервер
php artisan serve

# Во втором терминале - Vite dev server
npm run dev
```

Откройте в браузере: `http://localhost:8000`

### 4. Сборка для продакшена

```bash
npm run build
```

## 📱 Структура лендинга

### 1. **Хедер** (`LandingHeader.vue`)
- Логотип "АйТиПлюс"
- Навигационное меню
- Кнопка "Записаться на пробный урок"
- Адаптивное мобильное меню
- Эффекты при скролле

### 2. **Hero секция** (`HeroSection.vue`)
- Главный заголовок с акцентом
- Подзаголовок с описанием
- CTA кнопка
- Анимированный код-блок

### 3. **Почему «АйТиПлюс»** (`WhyUsSection.vue`)
- 5 карточек с преимуществами
- Иконки и описания
- Hover-эффекты

### 4. **Как проходят занятия** (`HowItWorksSection.vue`)
- Информация о формате обучения
- Визуальное расписание недели

### 5. **Курсы по возрастам** (`CoursesSection.vue`)
- 3 карточки курсов:
  - Junior Code (8–10 лет)
  - Python Start (11–13 лет)
  - Web & AI (14–17 лет)

### 6. **Преподаватели** (`TeachersSection.vue`)
- Карточки с информацией о наставниках
- Опыт работы и мотивационные фразы

### 7. **Отзывы** (`ReviewsSection.vue`)
- Отзывы родителей и учеников
- Звёздные рейтинги

### 8. **Почему программирование важно** (`WhyProgrammingSection.vue`)
- Информационный блок о важности навыка
- Преимущества обучения

### 9. **Формат и расписание** (`ScheduleSection.vue`)
- Гибкий график
- Удобные форматы обучения

### 10. **Форма записи** (`FormSection.vue`)
- Форма для записи на пробный урок
- Валидация полей
- Уведомления об успешной отправке

### 11. **Контакты** (`ContactsSection.vue`)
- Телефон, WhatsApp, Telegram
- Email
- Адрес (с плейсхолдером для карты)
- Социальные сети
- Ссылки на документы

### 12. **Футер** (`LandingFooter.vue`)
- Копирайт
- Декоративный текст

## 🎨 Особенности дизайна

- **Современный UI/UX**: Градиенты, тени, плавные анимации
- **Адаптивность**: Полностью адаптивный дизайн для всех устройств
- **Цветовая схема**: Фиолетово-синяя палитра (primary, secondary, accent)
- **Типографика**: Inter для основного текста, JetBrains Mono для кода
- **Анимации**: Плавные появления при скролле, hover-эффекты
- **Tailwind CSS**: Utility-first подход для быстрой разработки

## ⚙️ Функциональность

### Vue.js компоненты:

- ✅ Реактивные данные и состояние
- ✅ Плавная прокрутка к секциям
- ✅ Мобильное меню с анимацией
- ✅ Обработка формы записи
- ✅ Уведомления об успешной отправке
- ✅ Эффекты при скролле хедера
- ✅ Компонентная архитектура

## 🔧 Настройка

### Изменение контактов

Отредактируйте компонент `ContactsSection.vue`:

```vue
const contacts = [
    {
        icon: '📞',
        label: 'Телефон',
        value: '+7 (999) 123-45-67',
        href: 'tel:+79991234567',
    },
    // ...
];
```

### Интеграция карты

Замените плейсхолдер карты в `ContactsSection.vue`:

**Яндекс.Карты:**
```vue
<div id="map" class="w-full h-96 rounded-2xl"></div>
```

**Google Maps:**
```vue
<iframe 
    src="https://www.google.com/maps/embed?pb=YOUR_EMBED_CODE" 
    class="w-full h-96 rounded-2xl border-0"
    allowfullscreen
></iframe>
```

### Настройка формы

Для отправки формы на сервер отредактируйте `FormSection.vue`:

```vue
const handleSubmit = async () => {
    try {
        const response = await fetch('/api/enroll', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').content
            },
            body: JSON.stringify(form)
        });
        // Обработка ответа
    } catch (error) {
        // Обработка ошибки
    }
};
```

### Изменение цветов

Настройте цвета в `tailwind.config.js`:

```js
primary: {
    DEFAULT: '#6366f1',
    // ...
}
```

## 📱 Адаптивность

Лендинг полностью адаптирован для:
- 📱 Мобильных устройств (320px+)
- 📱 Планшетов (768px+)
- 💻 Десктопов (1024px+)
- 🖥️ Больших экранов (1440px+)

## 🌐 Браузерная поддержка

- Chrome (последние версии)
- Firefox (последние версии)
- Safari (последние версии)
- Edge (последние версии)

## 📝 Дальнейшие улучшения

- [ ] Интеграция с CRM системой
- [ ] Онлайн-платёж
- [ ] Календарь записи
- [ ] Видео-отзывы
- [ ] Блог/Новости
- [ ] Личный кабинет
- [ ] Чат-бот для консультаций
- [ ] Backend API для формы записи
- [ ] Админ-панель для управления контентом

## 🛠️ Команды разработки

```bash
# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Линтинг кода
npm run lint

# Форматирование кода
npm run format

# Проверка форматирования
npm run format:check
```

## 📄 Лицензия

© 2025 АйТиПлюс. Все права защищены.

---

Сделано с ❤️ для будущих программистов.
